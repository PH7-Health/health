import { saveCheckInAction } from "@/app/(app)/actions";
import { requireUser } from "@/lib/auth/session";
import { getTodayView } from "@/lib/life-os/service";

export default async function CheckInPage() { const user = await requireUser(); const state = await getTodayView(user.id); const entries = new Map(state.entries.map((entry) => [entry.metricDefinitionId, entry.valueNumber])); return <div className="page narrow"><section className="hero compact"><div><p className="eyebrow">Daily check-in</p><h2>Under two minutes. Only useful signal.</h2><p>Leave a field blank when you do not know it. Missing data remains unknown.</p></div><div className="score simple"><strong>{state.score}</strong><span>current score</span></div></section><form action={saveCheckInAction} className="panel form"><div className="metric-grid">{state.metrics.map((metric) => <label key={metric.id}><span>{metric.name}{metric.unit ? ` · ${metric.unit}` : ""}</span><input name={metric.key} type="number" step={metric.valueType === "SCALE_1_10" ? 1 : "any"} min={metric.valueType === "SCALE_1_10" ? 1 : undefined} max={metric.valueType === "SCALE_1_10" ? 10 : undefined} defaultValue={entries.get(metric.id) ?? ""} /></label>)}</div><label><span>Short reflection</span><textarea name="reflection" rows={4} defaultValue={state.checkIn?.reflection ?? ""} placeholder="What explains today?" /></label><div className="form-footer"><p>Score and drift rules refresh after save.</p><button>Save check-in</button></div></form></div>; }
